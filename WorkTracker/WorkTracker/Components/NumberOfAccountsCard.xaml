<UserControl x:Class="WorkTracker.Components.NumberOfAccountsCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkTracker.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card Margin="10"
                             materialDesign:ElevationAssist.Elevation="Dp3"
                             Padding="8"
                             Background="{DynamicResource MaterialDesignCardBackground}"
                             Foreground="{DynamicResource MaterialDesignBody}"
                             UniformCornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />

                </Grid.RowDefinitions>
                <TextBlock Text="{Binding TopLabel}"
                           FontSize="17"
                           Foreground="{DynamicResource SecondaryHueMidBrush}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap" />
                <TextBlock Text="{Binding NumberActive}"
                           Grid.Row="1"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           VerticalAlignment="Center"
                           FontSize="40"
                           FontWeight="Bold"
                           HorizontalAlignment="Center" />
                <StackPanel Orientation="Horizontal"
                            Grid.Row="2"
                            HorizontalAlignment="Center">
                    <Label Content="{DynamicResource NumberOfDeactivatedAccounts}"
                           Foreground="{DynamicResource SecondaryHueMidBrush}"
                           VerticalAlignment="Center"
                           FontSize="17" />
                    <Label Content="{Binding NumberOfDeactivatedAccounts}"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           VerticalAlignment="Center"
                           FontSize="17" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
